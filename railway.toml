[build]
builder = "nixpacks"

[build.envs]
NIXPACKS_NODE_VERSION = "20"

[deploy]
startCommand = "cd backend && npm run build && npm run start"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
name = "0friction-solver"
